# Core dependencies
pandas>=2.0.0
numpy>=1.20.0
tqdm>=4.60.0
regex>=2023.0.0
anytree>=2.8.0
tiktoken>=0.5.0
scikit-learn>=1.0.0

# Optional ML dependencies (used conditionally)
sentence-transformers>=2.0.0
torch>=1.9.0

# API clients (used conditionally based on API type)
openai>=1.0.0
google-generativeai>=0.3.0
vertexai>=1.0.0
anthropic>=0.3.0
vllm>=0.2.0

# Standard library modules (no installation needed)
# - os, json, time, traceback, subprocess, argparse, re, random
# - concurrent.futures (built-in)